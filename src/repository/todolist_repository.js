const { Todolist } =  require('./model/todolist_model.js')
class TodolistRepository {
    // constructor(todolistModel) {
    //     this.todolistModel = todolistModel
    // }

    async CreateTodolist(todolist) {
        Todolist.
    }
}
